#include "EzDebugHelper.h"
#include <stdlib.h>
#include <stdio.h>
#include <stdarg.h>

void EzDebugOutput(const char* pcFormat,...)
{

    EZASSERT(pcFormat);

    //va_list Args;
    //va_start(Args, pcFormat);
    //int iRet = EzVsnprintf(pcDest, stDestSize, stCount, pcFormat, Args);
    //va_end(Args);

    //return iRet;

	//    OutputDebugString(tmp);
//	OutputDebugString("\r\n");
}